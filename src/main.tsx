import React from "react";
import ReactDOM from "react-dom/client";
import PromptGamifiedAssistant from "../PromptGamifiedAssistant";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PromptGamifiedAssistant />
  </React.StrictMode>
);
